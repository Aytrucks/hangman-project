#pragma once
#include <iostream>
#include <vector>
using namespace std;

void greet();
void displayfail(int fails);
void Print(string string, bool top = true, bool bot = true);
void DrawHangman(int fail);
